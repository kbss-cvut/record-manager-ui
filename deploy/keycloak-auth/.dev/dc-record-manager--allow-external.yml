# Allows CORS requests from localhost dev servers (ports 4173, 5173) to record-manager-server.
services:
  record-manager-server:
    environment:
      security.cors.allowedOrigins: "http://localhost:4173,http://localhost:5173"
